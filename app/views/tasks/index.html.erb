<h1> All the Tasks! </h1>

<ul>
  <% @tasks.each do |task| %>
   <li><%= link_to task.title, task_path(task)  %></li>
     <%# <%= link_to task.title, show_path(task.id)  %>
  <% end %>
</ul>

<%= link_to 'create a task here!',  tasks_new_path %>
